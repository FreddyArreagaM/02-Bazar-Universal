import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: '02-bazar-universal',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
